# csv-profiler
profiles any messy CSV/Excel: column types, null rates, duplicate keys, date-format inconsistencies, encoding issues, outliers. Outputs a data-quality report + suggested cleanup steps.
